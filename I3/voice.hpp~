#ifndef INCLUDE_GUARD_VOICE_HPP
#define INCLUDE_GUARD_VOICE_HPP
#include <stdio.h>
#include <stdlib.h>

#define cut_volume -50

char change_mode(char *buf) {
  for (char c : buf) {
    if (c > cut_volume) return 0;
  }
  return 1;
}
